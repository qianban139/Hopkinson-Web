# Hopkinson-Web v1.1 备案前发布 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 通过环境变量短路鉴权解封访问 + Vercel 默认域名部署 + 二维码生成 + 整理冗余文件 + 把 BP-ANN / 围压 PID / CT 裂隙三块研究模块成品化 + 三 agent 并行审计，发布 v1.1。

**Architecture:** 单仓库 React 19 + TypeScript + Vite 7 + Zustand。无单元测试框架，验证以 `npm run dev` 浏览器手测 + `npm run build` + `npm run lint` 为门禁。Commits 按子项目划分，每个 commit 自包含可还原。

**Tech Stack:** React 19 / TypeScript 5.9 / Vite 7 / Tailwind / Zustand / ECharts / Framer Motion / Three.js / qrcode (新增)

**Spec reference:** `docs/superpowers/specs/2026-05-12-v1.1-release-design.md`

---

## File Structure 总览

### 新建文件

| 路径 | 责任 |
|---|---|
| `scripts/genQRCode.ts` | 用 `qrcode` 包根据 `QR_URL` 环境变量生成 PNG + SVG |
| `docs/deployment/oss-assets.md` | 阿里云 OSS bucket 资产清单 + 上传规范 |
| `docs/physics-engine/bp-network.md` | BP-ANN 本构预测算法说明 |
| `docs/physics-engine/confining-pressure-pid.md` | PID 围压闭环控制说明 |
| `docs/visualization/ct-fissure-extraction.md` | CT 裂隙提取 pipeline 与论文差异声明 |
| `docs/superpowers/cleanup-checklist-2026-05-12.md` | 待删文件清单（用户审核后手动执行） |
| `docs/superpowers/audits/2026-05-12-bp-pid-ct-audit.md` | E 项 agents 审计汇总 |
| `public/qr-vercel.png` / `.svg` | 二维码（脚本生成产物） |

### 修改文件（v1.1 工作）

| 路径 | 改动责任 |
|---|---|
| `src/components/ProtectedRoute.tsx` | 顶部读 VITE_AUTH_DISABLED 短路 |
| `src/store/useAuthStore.ts` | 模块底部 hydrate 块前置短路 |
| `src/components/Navbar.tsx` | 用户菜单按环境变量隐藏 |
| `.env.example` | 新增 VITE_AUTH_DISABLED |
| `.gitignore` | 新增 `public/assets/videos/` |
| `src/pages/MultiField.tsx` | 插入 PIDServoPanel Section |
| `package.json` | 新增 qrcode + tsx devDependency |
| `CLAUDE.md` / `CHANGELOG.md` / `docs/README.md` | 同步索引与服务路径 |

### 工作树预存改动（Phase 0 分类提交，与 v1.1 工作隔离）

| 路径 | 归属 commit |
|---|---|
| `src/pages/AIControl.tsx`, `src/pages/SystemMonitor.tsx` | chore: lint 清理 |
| `src/components/Navbar.tsx`（预存改动部分） | feat(navbar): 首页一级版块 |
| `src/pages/Login.tsx`, `src/store/useAuthStore.ts`（guest mode 部分）, `hopkinson-backend/.env.example`, `hopkinson-backend/db/seed.py` | feat(auth): 评委演示账号 + 游客模式 + 后端 CORS 示例 |
| `src/pages/Home.tsx`, `src/pages/Teaching.tsx`, `src/features/teaching-system/knowledgeData.ts` | feat(home): 研究亮点 + Teaching 深链 |
| `src/features/material-analysis/ConstitutiveFittingPanel.tsx`, `src/features/material-analysis/BPPredictionPanel.tsx`, `src/services/bpNetwork.ts`, `src/services/bpShpbPredictor.ts` | feat(material-analysis): BP-ANN 本构预测面板（D-BP commit） |
| `src/pages/MaterialAnalysis.tsx`, `src/features/ct-analysis/CTFissureExtractorPanel.tsx`, `src/services/imageProcessing/ctFissureExtractor.ts`, `src/data/literature.ts` | feat(material-analysis): CT 裂隙智能提取 Tab（D-CT commit） |
| `src/features/confining-pressure/PIDServoPanel.tsx`, `src/services/pidController.ts`, `src/services/confiningPressureSimulator.ts` | feat(multi-field): PID 围压闭环伺服（D-PID commit，含 MultiField 挂载） |
| `docs/competition-ppt/06-judge-review.pdf` (deleted) | chore(docs): 删除评委评审 PDF（Phase C） |
| 其他大件 (public/界面设计.png 等) | chore(repo): C-2 cleanup commit 后用户手动删 |

---

## Phase 0: 工作树预存改动分类提交

> 目的：在引入 v1.1 新改动前，把已有的零散改动按主题切分为 4 个独立 commit，让后续 commit 历史干净。

### Task 0.1: 配置 git autocrlf 抑制换行警告

**Files:**
- Modify: 全局 git config（不动文件）

- [ ] **Step 1: 设置 autocrlf=true（Windows 推荐）**

Run:
```bash
git config core.autocrlf true
```

- [ ] **Step 2: 验证设置**

Run:
```bash
git config --get core.autocrlf
```

Expected output: `true`

---

### Task 0.2: 提交 lint 清理（AIControl + SystemMonitor）

**Files:**
- Modify: `src/pages/AIControl.tsx` (已删 BG/CARD_BG 未用常量)
- Modify: `src/pages/SystemMonitor.tsx` (已删 History/Radio/CircleDot 等未用导入)

- [ ] **Step 1: 用 git add 仅暂存这两个文件**

Run:
```bash
git add src/pages/AIControl.tsx src/pages/SystemMonitor.tsx
```

- [ ] **Step 2: 验证 staged diff 只含 lint 删除**

Run:
```bash
git diff --cached --stat
```

Expected: 两个文件，仅 `-` 行无 `+` 行（或极少 `+` 行）。

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "chore: 清理未使用的导入与常量

- AIControl: 删除 BG / CARD_BG 未用颜色常量
- SystemMonitor: 删除 History / Radio / CircleDot / TrendingUp / Clock / ShieldAlert / ArrowRight 等未使用图标导入"
```

---

### Task 0.3: 提交 Navbar 首页一级版块

**Files:**
- Modify: `src/components/Navbar.tsx`（已添加 `首页` section 在导航最前）

**注意：本 Task 只提交 Navbar 中"加首页 section"的已有改动。A 项后续会再改 Navbar 隐藏用户菜单，那是 Phase A 的事。**

- [ ] **Step 1: 暂存 Navbar**

Run:
```bash
git add src/components/Navbar.tsx
```

- [ ] **Step 2: 验证 diff 仅含 navSections 数组扩展**

Run:
```bash
git diff --cached src/components/Navbar.tsx | head -30
```

Expected: 仅显示 `Home` icon import 与 `首页` section 的 `+` 行。

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "feat(navbar): 加入首页一级版块

将 4 大业务版块前置一个独立的"首页"入口，与 Logo 点击形成双路径
入口，便于评委从任意子页快速返回总览。"
```

---

### Task 0.4: 提交评委演示账号 + 游客模式 + 后端 seed

**Files:**
- Modify: `src/pages/Login.tsx`（加 JUDGE_USERS 快捷填充 + 游客按钮）
- Modify: `src/store/useAuthStore.ts`（加 isGuest / loginAsGuest，**不含** Phase A 的 VITE_AUTH_DISABLED 短路）
- Modify: `hopkinson-backend/.env.example`（CORS 生产示例）
- Modify: `hopkinson-backend/db/seed.py`（加 JUDGE_PASSWORD + 评委账号 seed）

- [ ] **Step 1: 暂存**

Run:
```bash
git add src/pages/Login.tsx src/store/useAuthStore.ts hopkinson-backend/.env.example hopkinson-backend/db/seed.py
```

- [ ] **Step 2: 提交**

Run:
```bash
git commit -m "feat(auth): 评委演示账号 + 离线游客模式 + 后端 seed

前端:
- Login 页加 judge1/2/3 快捷填充按钮（密码 Judge@2026）
- VITE_DEMO_MODE=true 时显示"游客离线演示"按钮
- useAuthStore 新增 isGuest / loginAsGuest，后端不可达时自动降级
后端:
- seed.py 写入 judge1/2/3 评委账号（Judge@2026）
- .env.example 增加 CORS 多 origin 示例"
```

---

### Task 0.5: 提交 Home 研究亮点 + Teaching 深链 + knowledgeData 扩充

**Files:**
- Modify: `src/pages/Home.tsx`（researchHighlights 数组与卡片）
- Modify: `src/pages/Teaching.tsx`（useSearchParams 解析 ?node= 自动选中）
- Modify: `src/features/teaching-system/knowledgeData.ts`（节点扩充）

- [ ] **Step 1: 暂存**

Run:
```bash
git add src/pages/Home.tsx src/pages/Teaching.tsx src/features/teaching-system/knowledgeData.ts
```

- [ ] **Step 2: 提交**

Run:
```bash
git commit -m "feat(home): 研究亮点卡片 + Teaching 深链定位

- Home 新增 researchHighlights section，展示 BP-ANN / CT 裂隙 / PID 三块论文复现
- 卡片点击直达 /analysis 或 /teaching?node=xxx
- Teaching 解析 ?node= 自动选中知识图谱节点
- knowledgeData 扩充对应节点"
```

- [ ] **Step 3: 验证 working tree 仅剩 v1.1 范围内待办**

Run:
```bash
git status --short
```

Expected: 只剩下：
- `M src/features/material-analysis/ConstitutiveFittingPanel.tsx`（D-BP commit 待处理）
- `M src/pages/MaterialAnalysis.tsx`（D-CT commit 待处理）
- `M src/data/literature.ts`（D-CT commit 待处理）
- `D docs/competition-ppt/06-judge-review.pdf`（Phase C 处理）
- `??` 新文件：BP、CT、PID 三块源文件 + UI 设计图等

---

### Task 0.6: 调整多场耦合温度规格到 -40~200°C

**Files:**
- Modify: `src/pages/MultiField.tsx`（SCENARIOS 数组 + 温度滑块 + 归一化函数）
- Modify: `src/features/ai-assistant/services/aiActionRegistry.ts:329, 332`

**背景**：项目预算表把"多场耦合温度范围"规格定为 -40~200°C（覆盖低温脆性到中温热软化），原代码用 20~1000°C 与之不符。SCENARIOS 中 10 个场景里 6 个温度超出新范围，需重设计为低温为主的工程场景。

- [ ] **Step 1: 替换 SCENARIOS 数组（7 个场景覆盖 -40~200）**

In `src/pages/MultiField.tsx`，替换 SCENARIOS 整段（约第 34-135 行）为：

```tsx
const SCENARIOS: ScenarioPreset[] = [
  {
    id: 'polar',
    title: '极地工程',
    tagline: 'T=-40°C, 冰载荷',
    icon: <Snowflake className="w-5 h-5" />,
    color: '#67E8F9',
    temperature: -40,
    stress: 600,
    emField: 15,
  },
  {
    id: 'cold-chain',
    title: '冷链运输',
    tagline: 'T=-25°C, σ=250MPa',
    icon: <Truck className="w-5 h-5" />,
    color: '#A5F3FC',
    temperature: -25,
    stress: 250,
    emField: 5,
  },
  {
    id: 'deep-sub',
    title: '海洋深潜器',
    tagline: 'T=2°C, P=110MPa',
    icon: <Anchor className="w-5 h-5" />,
    color: '#0EA5E9',
    temperature: 2,
    stress: 1100,
    emField: 20,
  },
  {
    id: 'room-temp',
    title: '常温动测基准',
    tagline: 'T=25°C, σ=200MPa',
    icon: <Thermometer className="w-5 h-5" />,
    color: '#94A3B8',
    temperature: 25,
    stress: 200,
    emField: 10,
  },
  {
    id: 'rail',
    title: '高铁轨道',
    tagline: 'T=60°C, v=350km/h',
    icon: <Train className="w-5 h-5" />,
    color: '#F97316',
    temperature: 60,
    stress: 450,
    emField: 65,
  },
  {
    id: 'ev-crash',
    title: 'EV 电池热失控',
    tagline: 'T=150°C, σ=350MPa',
    icon: <Battery className="w-5 h-5" />,
    color: '#1DD1A1',
    temperature: 150,
    stress: 350,
    emField: 80,
  },
  {
    id: 'oil-drill',
    title: '石油钻井',
    tagline: 'T=200°C, P=140MPa',
    icon: <Pickaxe className="w-5 h-5" />,
    color: '#A3E635',
    temperature: 200,
    stress: 1400,
    emField: 30,
  },
];
```

- [ ] **Step 2: 顶部 import 新增 Truck**

在 lucide-react 导入行（约第 4-8 行）加入 `Truck`。完整目标 import 行：

```tsx
import {
  Thermometer, Zap, Activity, Mountain, Rocket, Atom, Battery,
  ArrowRight, Send, Play, RotateCcw, AlertTriangle,
  Anchor, Train, Shield, Pickaxe, Snowflake, Flame, Truck,
} from 'lucide-react';
```

（`Mountain` / `Rocket` / `Atom` / `Shield` / `Flame` 已不再被场景使用，但可能被其他 UI 引用，保留无害；若 lint 报 unused 在 Phase 0.2 已处理过类似清理。）

- [ ] **Step 3: 温度初始值 + 重置值**

替换第 367 行 `useState(300)` 为 `useState(25)`：
```tsx
const [temperature, setTemperature] = useState(25);
```

替换第 449 行 `setTemperature(300)` 为 `setTemperature(25)`：
```tsx
setTemperature(25);
```

- [ ] **Step 4: 温度滑块 min/max**

替换第 642-644 行 `min={20} max={1000} step={5}` 为：
```tsx
                min={-40}
                max={200}
                step={5}
```

- [ ] **Step 5: 归一化函数（8 处 `temperature / 1000`）**

将所有 `temperature / 1000` 改为 `((temperature + 40) / 240)`（把 -40~200 映射到 0~1）。涉及行：
- 第 153 行 `const tNorm = temperature / 1000;`
- 第 392 行 `const t = temperature / 1000;`
- 第 400 行 `const tDev = thermalSoftening ? (temperature / 1000) * 18 : 0;`
- 第 412 行 `const tNorm = temperature / 1000;`
- 第 827 行 `Math.round((temperature / 1000 + stress / 2000) / 2 * 100)`
- 第 834 行 `animate={{ width: \`${Math.round((temperature / 1000 + stress / 2000) / 2 * 100)}%\` }}`
- 第 859 行 `Math.round((emField / 100 + temperature / 1000) / 2 * 100)`
- 第 866 行 `animate={{ width: \`${Math.round((emField / 100 + temperature / 1000) / 2 * 100)}%\` }}`

每处把 `temperature / 1000` 替换为 `((temperature + 40) / 240)`。

- [ ] **Step 6: aiActionRegistry 描述与 range**

在 `src/features/ai-assistant/services/aiActionRegistry.ts:329` 替换：
```ts
  description: '设置多场耦合实验的三场参数（温度、应力、电磁场）。温度范围-40~200°C，应力范围0-2000MPa，电磁场范围0-100T',
```

第 332 行：
```ts
    { name: 'temperature', type: 'number', description: '温度(°C)', required: false, range: [-40, 200] },
```

- [ ] **Step 7: 浏览器验证**

Run:
```bash
npm run dev
```

打开 `/multifield`：
- 看到 7 个场景卡（极地/冷链/深潜/常温/高铁/EV/石油），无超温场景
- 温度滑块范围 -40 到 200，步长 5，默认值 25
- 点 "极地工程" 预设：温度跳到 -40，UI 进度条 (`(temperature+40)/240` 归一化) 正常显示
- 点 "石油钻井"：温度跳到 200，进度条接近满

Ctrl+C 停止。

- [ ] **Step 8: 提交**

Run:
```bash
git add src/pages/MultiField.tsx src/features/ai-assistant/services/aiActionRegistry.ts
git commit -m "chore(multi-field): 调整温度规格到 -40~200°C 对齐预算表

按项目预算表"多场耦合温度范围"规格调整：

- 温度滑块 min={20} max={1000} → min={-40} max={200}
- 初始值/重置值 300 → 25 (常温基准)
- 8 处 temperature/1000 归一化 → (temperature+40)/240
- SCENARIOS 重设计为 7 个 -40~200 内的工程场景：
  极地 -40 / 冷链 -25 / 深潜 2 / 常温 25 /
  高铁 60 / EV 150 / 石油钻井 200
- aiActionRegistry 温度描述与 range 同步更新"
```

---

## Phase A: 登录暂停（环境变量短路）

### Task A.1: 在 .env.example 加 VITE_AUTH_DISABLED

**Files:**
- Modify: `.env.example`（前端，不是 hopkinson-backend/.env.example）

- [ ] **Step 1: 检查当前 .env.example 结构**

Run:
```bash
head -30 .env.example
```

- [ ] **Step 2: 在文件末尾追加新区块**

在 `.env.example` 最后一行后追加：

```env

# ─── 鉴权门禁 ───
# 设为 true 时绕过 ProtectedRoute 与 useAuthStore.hydrate()，所有页面无需登录直达。
# 备案完成 + 后端可达后改为 false 或删除此变量。
VITE_AUTH_DISABLED=true
```

- [ ] **Step 3: 同步本地 .env（不提交）**

如果存在 `.env`，手动添加同行 `VITE_AUTH_DISABLED=true`。`.env` 已被 .gitignore 不会提交。

---

### Task A.2: ProtectedRoute 顶部短路

**Files:**
- Modify: `src/components/ProtectedRoute.tsx:14-30`

- [ ] **Step 1: 在 `export default function ProtectedRoute` 函数体顶部插入短路**

Replace `src/components/ProtectedRoute.tsx` line 14-21 from:

```tsx
export default function ProtectedRoute({ children, adminOnly = false }: Props) {
  const location = useLocation();
  const user = useAuthStore((s) => s.user);
  const token = useAuthStore((s) => s.token);
  const hydrating = useAuthStore((s) => s.hydrating);

  // 1. 首次加载仍在校验 token -> 显示 skeleton,不跳转
  if (hydrating) {
```

To:

```tsx
export default function ProtectedRoute({ children, adminOnly = false }: Props) {
  // 备案期间短路：环境变量为 true 时全部放行，不读 auth store
  if (import.meta.env.VITE_AUTH_DISABLED === 'true') {
    return <>{children}</>;
  }

  const location = useLocation();
  const user = useAuthStore((s) => s.user);
  const token = useAuthStore((s) => s.token);
  const hydrating = useAuthStore((s) => s.hydrating);

  // 1. 首次加载仍在校验 token -> 显示 skeleton,不跳转
  if (hydrating) {
```

- [ ] **Step 2: 验证 TypeScript 无报错**

Run:
```bash
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -i "ProtectedRoute" | head -5
```

Expected: 无输出（无类型错误）。

---

### Task A.3: useAuthStore hydrate 前置短路

**Files:**
- Modify: `src/store/useAuthStore.ts:163-169`

- [ ] **Step 1: 替换模块底部的初始化块**

Replace `src/store/useAuthStore.ts` 第 163-169 行从：

```ts
// ─── 初始化: 挂监听 + 执行 hydrate ─────────────────
// 仅在浏览器环境下运行(SSR 安全)
if (typeof window !== 'undefined') {
  window.addEventListener(AUTH_UNAUTHORIZED_EVENT, () => {
    useAuthStore.getState().logout();
  });
  // hydrate 异步进行,ProtectedRoute 读 hydrating 显示 Skeleton
  useAuthStore.getState().hydrate();
}
```

To：

```ts
// ─── 初始化: 挂监听 + 执行 hydrate ─────────────────
// 仅在浏览器环境下运行(SSR 安全)
if (typeof window !== 'undefined') {
  // 备案期间短路：跳过 hydrate 与 /api/auth/me，避免空跑后端
  if (import.meta.env.VITE_AUTH_DISABLED === 'true') {
    useAuthStore.setState({ hydrating: false });
  } else {
    window.addEventListener(AUTH_UNAUTHORIZED_EVENT, () => {
      useAuthStore.getState().logout();
    });
    // hydrate 异步进行,ProtectedRoute 读 hydrating 显示 Skeleton
    useAuthStore.getState().hydrate();
  }
}
```

- [ ] **Step 2: 验证 TypeScript 无报错**

Run:
```bash
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -i "useAuthStore" | head -5
```

Expected: 无输出。

---

### Task A.4: Navbar 隐藏用户菜单

**Files:**
- Modify: `src/components/Navbar.tsx:351-391`

- [ ] **Step 1: 在 Navbar 函数体内增加 authDisabled 常量**

在 `export default function Navbar()` 函数体顶部（`const [isScrolled, setIsScrolled] = useState(false);` 之前）插入：

```tsx
  const authDisabled = import.meta.env.VITE_AUTH_DISABLED === 'true';
```

- [ ] **Step 2: 用 authDisabled 包裹用户菜单的显隐**

Find `{user && (` 在第 351 行附近（桌面端 DropdownMenu）和第 469 行附近（移动端用户卡片），将两处的 `{user && (` 改为 `{user && !authDisabled && (`。

桌面端 (第 351 行附近)：
```tsx
              {user && !authDisabled && (
                <DropdownMenu>
```

移动端 (第 469 行附近，在 Sheet 内)：
```tsx
                  {user && !authDisabled && (
                    <div className="px-3 mt-4 pt-4 border-t border-white/5">
```

- [ ] **Step 3: 验证 lint**

Run:
```bash
npm run lint -- --max-warnings=0 src/components/Navbar.tsx
```

Expected: 无 error，warning 数 ≤ 现有水平。

---

### Task A.5: 浏览器验证 A 全流程

- [ ] **Step 1: 启动 dev server**

Run:
```bash
npm run dev
```

后台运行；待终端打印 `Local:   http://localhost:5173/` 后继续。

- [ ] **Step 2: 验证访问 `/` 不跳 `/login`**

打开浏览器 `http://localhost:5173/`，预期：
- 直接看到 Home 页面（不闪过 Skeleton 不跳 /login）
- 顶部导航无用户头像与"退出"按钮
- DevTools Network 面板**无** `/api/auth/me` 请求

- [ ] **Step 3: 验证 `/login` 仍可访问（管理员入口）**

浏览器访问 `http://localhost:5173/login`，预期：能看到登录页（不跳回 `/`）。

- [ ] **Step 4: 验证所有受保护路由可达**

依次访问以下 URL，每个都能直接渲染（不跳 /login）：
- `/lab`
- `/analysis`
- `/monitor`
- `/teaching`
- `/multifield`
- `/ai-control`

- [ ] **Step 5: 停止 dev server**

回到终端 Ctrl+C 终止。

---

### Task A.6: Commit Phase A

- [ ] **Step 1: 暂存 A 项改动**

Run:
```bash
git add .env.example src/components/ProtectedRoute.tsx src/store/useAuthStore.ts src/components/Navbar.tsx
```

- [ ] **Step 2: 检查 staged diff 只含 4 个文件**

Run:
```bash
git diff --cached --stat
```

Expected: 列出 4 个文件，无其他。

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "feat(auth): VITE_AUTH_DISABLED 环境变量短路鉴权

备案期间后端 hopkinson-backend 不可达，前端无法 /api/auth/me 校验 token，
导致 ProtectedRoute 永远跳 /login。此 commit 用环境变量短路：

- ProtectedRoute 顶部检查 VITE_AUTH_DISABLED，true 时直接放行
- useAuthStore 模块初始化时跳过 hydrate()，避免空跑 /api/auth/me
- Navbar 用户菜单按 authDisabled 隐藏，避免暴露登录态 UI
- /login /register 路由保留，管理员可手动访问

回滚成本：备案后改 env 变量为 false 或删除即可，零代码改动。"
```

---

## Phase B: Vercel + 二维码 + OSS 文档

### Task B.1: 安装 qrcode 与 tsx 依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装 devDependencies**

Run:
```bash
npm i -D qrcode @types/qrcode tsx
```

- [ ] **Step 2: 验证 package.json 已更新**

Run:
```bash
grep -E "qrcode|tsx" package.json
```

Expected: 三个 devDependencies 行均出现。

---

### Task B.2: 编写 scripts/genQRCode.ts

**Files:**
- Create: `scripts/genQRCode.ts`

- [ ] **Step 1: 创建 scripts 目录（如不存在）**

Run:
```bash
mkdir -p scripts
```

- [ ] **Step 2: 写入脚本**

Create `scripts/genQRCode.ts` with:

```ts
/**
 * 二维码生成脚本
 *
 * 用法：
 *   npx tsx scripts/genQRCode.ts                              # 默认 Vercel 默认域名
 *   QR_URL=https://hopkinson-lab.com npx tsx scripts/genQRCode.ts   # 自定义域名
 *   QR_NAME=qr-lab QR_URL=https://hopkinson-lab.com npx tsx scripts/genQRCode.ts
 *
 * 输出：
 *   public/<QR_NAME>.png   512×512 PNG，深色前景 #0A2540，白底
 *   public/<QR_NAME>.svg   矢量版本
 */

import QRCode from 'qrcode';
import { resolve } from 'node:path';

const URL_TARGET = process.env.QR_URL ?? 'https://hopkinson-bar.vercel.app';
const NAME = process.env.QR_NAME ?? 'qr-vercel';
const OUT_DIR = resolve(process.cwd(), 'public');

async function main(): Promise<void> {
  const pngPath = `${OUT_DIR}/${NAME}.png`;
  const svgPath = `${OUT_DIR}/${NAME}.svg`;

  await QRCode.toFile(pngPath, URL_TARGET, {
    width: 512,
    margin: 2,
    color: { dark: '#0A2540', light: '#FFFFFF' },
    errorCorrectionLevel: 'H',
  });

  await QRCode.toFile(svgPath, URL_TARGET, {
    type: 'svg',
    margin: 2,
    color: { dark: '#0A2540', light: '#FFFFFF' },
    errorCorrectionLevel: 'H',
  });

  console.log(`✓ Generated QR for ${URL_TARGET}`);
  console.log(`  PNG: ${pngPath}`);
  console.log(`  SVG: ${svgPath}`);
}

main().catch((err) => {
  console.error('Failed to generate QR code:', err);
  process.exit(1);
});
```

---

### Task B.3: 运行脚本生成二维码

- [ ] **Step 1: 执行**

Run:
```bash
npx tsx scripts/genQRCode.ts
```

Expected output:
```
✓ Generated QR for https://hopkinson-bar.vercel.app
  PNG: <abs>/public/qr-vercel.png
  SVG: <abs>/public/qr-vercel.svg
```

- [ ] **Step 2: 验证文件**

Run:
```bash
ls -la public/qr-vercel.png public/qr-vercel.svg
```

Expected: 两个文件都存在，PNG ~5-15 KB，SVG ~3-8 KB。

- [ ] **Step 3: 用手机扫描验证**

打开 `public/qr-vercel.png`（用图片查看器）→ 手机扫码 → 应跳转到 `https://hopkinson-bar.vercel.app`。

---

### Task B.4: 编写 docs/deployment/oss-assets.md

**Files:**
- Create: `docs/deployment/oss-assets.md`

- [ ] **Step 1: 写入文档**

Create `docs/deployment/oss-assets.md` with:

````markdown
# 阿里云 OSS 资产托管

> 大体积视频/模型等静态资产**不**进 Vercel 构建产物，统一托管阿里云 OSS。

## Bucket 信息

| 字段 | 值 |
|---|---|
| Bucket | `hopkinson-assets` |
| Region | 杭州（cn-hangzhou） |
| Endpoint | `https://hopkinson-assets.oss-cn-hangzhou.aliyuncs.com` |
| 访问权限 | 公开读 |
| 鉴权 | 无（前端直引） |

## 目录结构

```
oss://hopkinson-assets/
├── videos/        # 实验演示视频 (.mp4, ≤ 50 MB / 文件)
└── models/        # 备用 3D 模型 (.gltf + .bin)（首选仍在 public/models/）
```

## 现有引用

| 资产 URL | 引用位置 |
|---|---|
| `videos/3Dmodel.mp4` | `src/pages/VirtualLab.tsx:1543` |
| `videos/xiaotiao.mp4` | `src/pages/Home.tsx:391` |

## 上传规范

1. 文件命名：小写英文 + 短横，禁止空格与中文。
2. 视频压缩：H.264 / 1080p / 2-5 Mbps / .mp4。
3. 上传后在 OSS 控制台确认 ACL = 公开读。
4. 前端引用统一写绝对 URL：
   ```ts
   const VIDEO_URL = 'https://hopkinson-assets.oss-cn-hangzhou.aliyuncs.com/videos/<file>.mp4';
   ```
5. 不要把视频原文件放到 `public/assets/videos/`（已加 .gitignore）。

## 跨域

- 当前为静态资源直引（video / img / link），**无跨域问题**。
- 若未来要 `fetch()` OSS 资源，需在 OSS 控制台为 bucket 配置 CORS 规则，允许 Vercel 域名 GET。

## 故障排查

- 视频加载 403：检查 ACL 是否被改成"私有"。
- 跨区域延迟高：考虑开启 OSS CDN 加速（cdn.oss-cn-hangzhou.aliyuncs.com），本次不做。
````

---

### Task B.5: Commit Phase B

- [ ] **Step 1: 暂存**

Run:
```bash
git add package.json package-lock.json scripts/genQRCode.ts public/qr-vercel.png public/qr-vercel.svg docs/deployment/oss-assets.md
```

- [ ] **Step 2: 验证 staged**

Run:
```bash
git diff --cached --stat
```

Expected: 6 个文件。

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "feat(deploy): 二维码生成脚本 + OSS 资产文档

- scripts/genQRCode.ts：通过 qrcode 包生成 PNG + SVG，URL 与文件名
  均由环境变量 QR_URL / QR_NAME 控制，未来接入自定义域名时复用
- public/qr-vercel.{png,svg}：当前默认指向 https://hopkinson-bar.vercel.app
- docs/deployment/oss-assets.md：阿里云 OSS bucket 资产清单 +
  现有引用位置 + 上传命名规范"
```

---

### Task B.6: 用户手动操作 — Vercel Dashboard

> 此 Task 由用户执行，无代码改动。仅作为 plan 的检查清单。

- [ ] **Step 1: 登录 Vercel Dashboard**

打开 https://vercel.com/dashboard，选择 hopkinson-web 项目。

- [ ] **Step 2: 添加生产环境变量**

Settings → Environment Variables → Add：
- Key: `VITE_AUTH_DISABLED`
- Value: `true`
- Environment: 勾选 Production（也可勾 Preview）

- [ ] **Step 3: 触发 Production 重新部署**

Deployments → 最新 production deployment → ⋯ → Redeploy → 确认。

或者：把 Phase A/B 的 commit 推到 main，Vercel 自动触发。

- [ ] **Step 4: 等待部署 Ready，验证线上**

部署完成（约 2-5 分钟）后，浏览器访问 `https://hopkinson-bar.vercel.app/`：
- 直达 Home 不跳 /login
- 手机扫 `public/qr-vercel.png` 也直达。

---

## Phase C: 项目整理（清单 + .gitignore + docs 同步）

### Task C.1: 写清理 checklist

**Files:**
- Create: `docs/superpowers/cleanup-checklist-2026-05-12.md`

- [ ] **Step 1: 写入清单**

Create `docs/superpowers/cleanup-checklist-2026-05-12.md` with:

````markdown
# v1.1 仓库清理 Checklist（2026-05-12）

> 由 Claude 生成，用户审核后**手动**执行。每项打勾后再提交。

## 建议删除（约 706 MB）

- [ ] `public/界面设计.png` — 6.7 MB UI 设计稿，非生产资产
- [ ] `public/实验图片.gif` — 11 KB 早期演示
- [ ] `public/assets/videos/` 整目录 — 699 MB；视频走阿里云 OSS（详见 docs/deployment/oss-assets.md），代码内零引用（grep src/ 已确认）
- [ ] `context/WEBSITE_DESIGN_LOGIC.md` — 已被 CLAUDE.md 取代
- [ ] `context/pre.md` — 早期 prompt 残留
- [ ] `context/wave_data.json` — 早期 mock，未被引用
- [ ] `context/Hopkinson_Bar_Web_Custom_Instructions.md` — 与 CLAUDE.md 重复
- [ ] `info.md` — 早期 README 雏形
- [ ] `.claude/Hopkinson_Bar_Web_Custom_Instructions.md` — 已被 CLAUDE.md 取代
- [ ] `.claude/context.md` — 已被 CLAUDE.md 取代

如全部确认，可一次性执行：

```bash
git rm -r --cached public/assets/videos
rm -rf public/assets/videos public/界面设计.png public/实验图片.gif
rm -rf context info.md
rm -f .claude/Hopkinson_Bar_Web_Custom_Instructions.md .claude/context.md
```

## 不动（保留）

- `public/models/new_hopkinson.bin + .gltf` — 41 MB，3D 数字孪生在用（GLTFModel.tsx）
- `dist/` — 已在 .gitignore（如未在则添加）
- `node_modules/` — 已在 .gitignore

## 评估后再决定

无。
````

---

### Task C.2: 更新 .gitignore

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 检查当前 .gitignore**

Run:
```bash
cat .gitignore
```

- [ ] **Step 2: 追加新条目**

在 `.gitignore` 末尾追加：

```gitignore

# 大体积视频走阿里云 OSS，本地副本仅供开发，不进仓库
public/assets/videos/
```

- [ ] **Step 3: 验证**

Run:
```bash
git check-ignore public/assets/videos/test.mp4
```

Expected output:
```
public/assets/videos/test.mp4
```
（表明已被忽略）

---

### Task C.3: 更新 docs/README.md 索引

**Files:**
- Modify: `docs/README.md`

- [ ] **Step 1: 查看当前 docs/README.md**

Run:
```bash
cat docs/README.md
```

- [ ] **Step 2: 在合适位置插入 v1.1 新文档**

在物理引擎章节追加：
- `physics-engine/bp-network.md` — BP-ANN 本构预测（v1.1）
- `physics-engine/confining-pressure-pid.md` — PID 围压闭环（v1.1）

在可视化章节追加：
- `visualization/ct-fissure-extraction.md` — CT 裂隙智能提取（v1.1）

在部署章节追加：
- `deployment/oss-assets.md` — 阿里云 OSS 资产托管（v1.1）

具体编辑根据 docs/README.md 现有 markdown 结构 fold-in，保持风格一致。

---

### Task C.4: 更新 CLAUDE.md 关键文件路径

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 找到"物理引擎与算法"区块**

Run:
```bash
grep -n "shpbPhysicsEngine" CLAUDE.md
```

- [ ] **Step 2: 在 `signalProcessing/` 行后追加**

替换 CLAUDE.md 中该区块（首段"物理引擎与算法"列表），在 `- src/services/reportGenerator.ts` 之后追加：

```markdown
- `src/services/bpNetwork.ts` — BP 神经网络（单隐层 MLP，纯 JS）
- `src/services/bpShpbPredictor.ts` — BP 驱动的 SHPB 本构预测
- `src/services/pidController.ts` — PID 控制器
- `src/services/confiningPressureSimulator.ts` — 围压二阶系统仿真
- `src/services/imageProcessing/ctFissureExtractor.ts` — 煤岩 CT 裂隙提取（Otsu + 形态学 + 连通域）
```

---

### Task C.5: 更新 CHANGELOG.md 新增 v1.1.0 条目

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 查看现有 CHANGELOG.md**

Run:
```bash
head -20 CHANGELOG.md
```

- [ ] **Step 2: 在最上方新增 v1.1.0 段**

在 v1.0.0 上方插入：

```markdown
## [1.1.0] - 2026-05-12

### 新增

- **BP-ANN 本构预测**：单隐层神经网络替代/对照传统本构模型，可外推未拟合应变率
  - `src/services/bpNetwork.ts`、`bpShpbPredictor.ts`
  - `src/features/material-analysis/BPPredictionPanel.tsx`
  - 接入 MaterialAnalysis 参数拟合 tab 的 'bp-ann' 模式
- **PID 围压闭环伺服**：二阶被控对象 + 时滞 + Z-N 整定三档预设
  - `src/services/pidController.ts`、`confiningPressureSimulator.ts`
  - `src/features/confining-pressure/PIDServoPanel.tsx`
  - 挂载于 MultiField 页面，目标围压 50 MPa
- **煤岩 Micro-CT 裂隙提取**：Otsu + 形态学 + 连通域 pipeline
  - `src/services/imageProcessing/ctFissureExtractor.ts`
  - `src/features/ct-analysis/CTFissureExtractorPanel.tsx`
  - 接入 MaterialAnalysis 新 Tab "CT 裂隙提取"
- **首页研究亮点**：Home 新增 researchHighlights 三卡，直链到上述功能
- **二维码脚本**：`scripts/genQRCode.ts` 生成 public/qr-vercel.png/svg

### 变更

- **暂停登录**：新增 `VITE_AUTH_DISABLED` 环境变量短路 ProtectedRoute + useAuthStore.hydrate()，备案期间所有访客直达，无需登录
- **评委演示账号**：Login 加 judge1/2/3 快捷填充 + 游客离线模式（VITE_DEMO_MODE）
- **Navbar**：加入"首页"一级版块

### 文档

- 新增 `docs/physics-engine/bp-network.md`
- 新增 `docs/physics-engine/confining-pressure-pid.md`
- 新增 `docs/visualization/ct-fissure-extraction.md`
- 新增 `docs/deployment/oss-assets.md`
- 新增 `docs/superpowers/audits/2026-05-12-bp-pid-ct-audit.md`（Agent 审计）

### 清理

- 删除 `docs/competition-ppt/06-judge-review.pdf`（2.1 MB）
- 清理 `AIControl.tsx` / `SystemMonitor.tsx` 未用导入

```

---

### Task C.6: Commit Phase C-1（清单 + .gitignore + 文档索引）

- [ ] **Step 1: 暂存**

Run:
```bash
git add docs/superpowers/cleanup-checklist-2026-05-12.md .gitignore docs/README.md CLAUDE.md CHANGELOG.md
```

- [ ] **Step 2: 同时把已删的 06-judge-review.pdf 纳入**

Run:
```bash
git add docs/competition-ppt/06-judge-review.pdf
```

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "chore(repo): v1.1 仓库整理 — 清单/索引/CHANGELOG/.gitignore

- 生成 cleanup-checklist-2026-05-12.md，列出 706 MB 待删冗余资产
  （由用户审核后手动 rm，本 commit 不删任何文件）
- .gitignore 增加 public/assets/videos/（视频走 OSS）
- 删除 docs/competition-ppt/06-judge-review.pdf（2.1 MB）
- docs/README.md 索引追加 v1.1 新增文档
- CLAUDE.md 关键路径补全 BP / PID / CT 新服务
- CHANGELOG.md 增加 v1.1.0 条目"
```

---

### Task C.7: 用户手动执行 cleanup checklist（可选，备案前可推迟）

> 此 Task 由用户审核 `docs/superpowers/cleanup-checklist-2026-05-12.md` 后手动执行；不在本计划自动化范围。

- [ ] **Step 1: 用户审核 checklist 决定保留/删除**
- [ ] **Step 2: 用户执行 `rm -rf` 与 `git rm --cached`**
- [ ] **Step 3: 用户 commit "chore(repo): 执行 cleanup-checklist-2026-05-12"**

---

## Phase D: BP / CT / PID 三块成品化

### Task D.1: 验证 BP 集成的工作树 diff

**Files:**
- Inspect: `src/features/material-analysis/ConstitutiveFittingPanel.tsx`
- Inspect: `src/features/material-analysis/BPPredictionPanel.tsx`（新文件，无 diff）
- Inspect: `src/services/bpNetwork.ts`、`src/services/bpShpbPredictor.ts`

- [ ] **Step 1: 完整 Read ConstitutiveFittingPanel diff**

Run:
```bash
git diff src/features/material-analysis/ConstitutiveFittingPanel.tsx
```

- [ ] **Step 2: 确认 'bp-ann' 已正确加入模型 tab**

预期 diff 含：
- 新 import `import BPPredictionPanel from './BPPredictionPanel';`
- type `ModeId = ConstitutiveModelId | 'bp-ann';`
- `selectedModel` 类型扩展为 `ModeId`
- `if (isBPMode)` 分支早返回 `<BPPredictionPanel />`
- `ModelSelectorTabs` 加入 'bp-ann' 按钮

- [ ] **Step 3: 启动 dev server 浏览器验证**

Run:
```bash
npm run dev
```

打开 `http://localhost:5173/analysis` → 选择任意材料 → 点 "参数拟合" tab → 找到模型选择条 → 切到 BP-ANN → 点 "训练 BP 网络" → 观察 loss 曲线下降、R² 上升、训练完成后 3 应变率泛化曲线显示。

- [ ] **Step 4: 停止 dev server**

终端 Ctrl+C。

---

### Task D.2: 写 docs/physics-engine/bp-network.md

**Files:**
- Create: `docs/physics-engine/bp-network.md`

- [ ] **Step 1: 写入文档**

Create `docs/physics-engine/bp-network.md` with:

````markdown
# BP-ANN 本构预测

> 论文来源：龙旭 等（2021）"基于人工神经网络的混凝土类材料 SHPB 动态压缩性能预测"
> 实现位置：`src/services/bpNetwork.ts` + `src/services/bpShpbPredictor.ts`
> UI 入口：MaterialAnalysis → 参数拟合 Tab → 模型选择 → BP-ANN

## 算法

### 网络结构

- **输入层**：3 维 `(应变 ε, 应变率 ε̇, 温度 T)`
- **隐层**：默认 12 个神经元，tansig 激活（`tanh(x)`）
- **输出层**：1 维 `应力 σ` (MPa)，purelin 线性激活
- **权重初始化**：Xavier，方差 `2 / (in + out)`
- **归一化**：输入与输出均用 Min-Max 独立归一化，推理时反归一化

### 训练

- **优化器**：小批量 SGD
- **学习率衰减**：`lr / sqrt(1 + 0.02·epoch)`
- **默认参数**：hiddenDim=12 / epochs=120 / lr=0.15 / batchSize=8
- **样本来源**：
  1. 选定材料的 `stressStrainSample`（典型 10-20 点）作为基准
  2. 用材料 J-C 参数 + 应变率扰动 (×/÷ 2.5 倍) + 温度扰动 (±50°C) 合成 `augmentFactor × 原始点数` 个样本

### 预测

```ts
const sigma: number = net.predict([strain, strainRate, temperature])[0];
```

返回的 σ 已反归一化为 MPa。

## 与传统本构对比

| 维度 | J-C / C-S / Z-A 等传统模型 | BP-ANN |
|---|---|---|
| 物理意义 | 强 — 每个参数有物理含义 | 弱 — 权重不可解释 |
| 拟合精度 | 受模型形式约束 | 可逼近任意非线性 |
| 外推能力 | 弱（外推应变率往往失真） | 受训练样本分布约束 |
| 训练成本 | 几次最小二乘 | 100-300 epochs |

## 局限（必读）

1. **训练数据本质源自 J-C 合成**：BP 在拟合 J-C，泛化上限即 J-C 模型本身的物理表达力。**不能**声称比传统本构"更准"，只能说"可外推到未显式拟合的应变率/温度工况"。
2. **浏览器主线程训练**：超过 200 epoch 会有 UI 卡顿（已用 `await setTimeout(0)` 每 5 epoch 让出，但仍是单线程）。
3. **无验证集**：当前是过拟合训练集的 R²，不是 hold-out 性能。

## 参考

- 龙旭, 等. 基于人工神经网络的混凝土类材料 SHPB 动态压缩性能预测[J]. 南京航空航天大学学报, 2021, 53(5).
````

---

### Task D.3: Commit D-BP

- [ ] **Step 1: 暂存 BP 相关全部文件**

Run:
```bash
git add src/services/bpNetwork.ts src/services/bpShpbPredictor.ts src/features/material-analysis/BPPredictionPanel.tsx src/features/material-analysis/ConstitutiveFittingPanel.tsx docs/physics-engine/bp-network.md
```

- [ ] **Step 2: 验证 staged**

Run:
```bash
git diff --cached --stat
```

Expected: 5 个文件（3 个新建 + 2 个修改/新建文档）。

- [ ] **Step 3: 提交**

Run:
```bash
git commit -m "feat(material-analysis): BP-ANN 神经网络本构预测面板

实现：
- bpNetwork.ts：单隐层 BP 网络（Xavier 初始化 / SGD / 学习率衰减）
- bpShpbPredictor.ts：用 J-C 参数扩充合成训练样本
- BPPredictionPanel.tsx：训练参数滑块 + Loss/R² 实时曲线 + 3 应变率泛化预测图
- ConstitutiveFittingPanel.tsx：模型选择 tab 加入 'bp-ann' 模式

文档：
- docs/physics-engine/bp-network.md：算法说明 + 与传统本构对比 + 局限声明

论文来源：龙旭 等 (2021), 南京航空航天大学学报 53(5)"
```

---

### Task D.4: 验证 CT 集成

**Files:**
- Inspect: `src/pages/MaterialAnalysis.tsx`
- Inspect: `src/features/ct-analysis/CTFissureExtractorPanel.tsx`（新文件）
- Inspect: `src/services/imageProcessing/ctFissureExtractor.ts`（新文件）
- Inspect: `src/data/literature.ts`

- [ ] **Step 1: Read MaterialAnalysis diff**

Run:
```bash
git diff src/pages/MaterialAnalysis.tsx | head -100
```

预期：
- 新 import `CTFissureExtractorPanel from '@/features/ct-analysis/CTFissureExtractorPanel'`
- 新 import `Microscope as MicroscopeIcon`
- TabsList 新增 `<TabsTrigger value="ct">`
- 新 `<TabsContent value="ct">` 渲染 `<CTFissureExtractorPanel />`

- [ ] **Step 2: Read literature.ts diff**

Run:
```bash
git diff src/data/literature.ts | head -50
```

预期：新增王登科 2024 文献条目（category=material-science 或 experimental-method）。

- [ ] **Step 3: 浏览器验证**

Run:
```bash
npm run dev
```

打开 `/analysis` → 任意材料 → 切到 "CT 裂隙提取" tab → 自动加载合成 CT → 点 "提取裂隙" → 三图（原图/掩膜/叠加）显示 → 评价指标卡（MIoU/F1 等）显示 → 直方图显示 Otsu 阈值线。

切到 "参考文献" tab → 选岩石/混凝土类材料 → 验证王登科 2024 条目出现。

- [ ] **Step 4: 停止 dev server**

---

### Task D.5: 写 docs/visualization/ct-fissure-extraction.md

**Files:**
- Create: `docs/visualization/ct-fissure-extraction.md`

- [ ] **Step 1: 写入文档**

Create `docs/visualization/ct-fissure-extraction.md` with:

````markdown
# 煤岩 Micro-CT 裂隙智能提取

> 论文参考：王登科 等（2024）"基于深度学习的煤岩 Micro-CT 裂隙智能提取与应用"
> 实现位置：`src/services/imageProcessing/ctFissureExtractor.ts`
> UI 入口：MaterialAnalysis → CT 裂隙提取 Tab

## **与论文的差异声明（必读）**

论文原方法使用 **MCSN（U-Net + VGG16 迁移学习 + DCAC 空洞卷积）**，需要 GPU 离线训练 + 标注数据集。

本项目为**纯前端浏览器演示版本**，**不是**论文原方法的复现，而是采用工程上经典的"传统+现代"混合 pipeline：

1. Otsu 自适应阈值（无监督二值化）
2. 形态学闭运算（连接细裂隙）
3. 形态学开运算（去噪点）
4. 连通域分析（按面积过滤）
5. 评价指标对比 Ground Truth（合成 CT 才有 GT）

适用场景：教学演示、快速预览、无需 GPU。不适合代替 MCSN 的真实工程应用。

## Pipeline 细节

### 1. Otsu 阈值

最大化类间方差：

```
σ_B²(t) = w0(t)·w1(t)·(μ0(t) - μ1(t))²
t* = argmax_t σ_B²(t)
```

### 2. 形态学

- **闭运算**：膨胀 → 腐蚀，连接断裂的裂隙线段
- **开运算**：腐蚀 → 膨胀，去除孤立小噪点
- 默认 3×3 方形 kernel，可在 UI 调到 9×9，闭/开各 0-4 次迭代

### 3. 连通域

DFS（深度优先搜索）遍历二值掩膜，每个连通分量计算面积，小于 `minArea`（默认 20 px）丢弃。

### 4. 评价指标

仅在合成 CT（有 GT）时计算：

| 指标 | 公式 |
|---|---|
| Pixel Accuracy | `(TP + TN) / (TP + TN + FP + FN)` |
| Precision | `TP / (TP + FP)` |
| Recall | `TP / (TP + FN)` |
| F1 | `2·P·R / (P + R)` |
| MIoU | `mean over classes of (TP / (TP + FP + FN))` |
| MPA | `mean over classes of (TP / (TP + FN))` |

## UI 行为

- 上传任意 PNG/JPG → 缩放到 ≤ 320 px → 显示原图，无评价指标
- 点"生成演示 CT" → 合成 320×320 灰度图（含裂隙状黑色随机曲线 + 椒盐噪声）+ 对应 GT
- 调形态学参数 + 反转开关 → 点 "提取裂隙" → 三联图 + 评价指标 + 直方图

## 参考

- 王登科, 等. 基于深度学习的煤岩 Micro-CT 裂隙智能提取与应用[J]. 2024.
````

---

### Task D.6: Commit D-CT

- [ ] **Step 1: 暂存**

Run:
```bash
git add src/services/imageProcessing/ctFissureExtractor.ts src/features/ct-analysis/CTFissureExtractorPanel.tsx src/pages/MaterialAnalysis.tsx src/data/literature.ts docs/visualization/ct-fissure-extraction.md
```

- [ ] **Step 2: 提交**

Run:
```bash
git commit -m "feat(material-analysis): 煤岩 Micro-CT 裂隙智能提取 Tab

实现：
- imageProcessing/ctFissureExtractor.ts：Otsu + 形态学闭/开 +
  DFS 连通域过滤 + 评价指标（PA / MPA / MIoU / P / R / F1）
- CTFissureExtractorPanel.tsx：演示合成 CT / 上传 / 三联图 / 直方图 UI
- MaterialAnalysis 加 Tab 6 'ct'
- literature.ts 加王登科 2024 文献条目

文档：
- docs/visualization/ct-fissure-extraction.md：算法 pipeline +
  与论文原 MCSN 方法的差异声明

论文参考：王登科 等 (2024)。本实现为浏览器演示版本，
非论文原 U-Net+VGG16+DCAC 深度学习方法。"
```

---

### Task D.7: 在 MultiField.tsx 挂载 PIDServoPanel

**Files:**
- Modify: `src/pages/MultiField.tsx`

- [ ] **Step 1: 查看 MultiField 当前结构定位插入点**

Run:
```bash
grep -n "SCENARIOS\|<Slider\|应力-应变\|应力应变" src/pages/MultiField.tsx | head -20
```

定位：找到 SCENARIOS 场景预设 grid 渲染结束的标签，然后是参数滑块或应力曲线开始处。

- [ ] **Step 2: 在文件顶部 import 区添加**

在 MultiField.tsx 现有 import 块末尾（第 17 行 `import type ...` 之前）添加：

```tsx
import PIDServoPanel from '@/features/confining-pressure/PIDServoPanel';
```

- [ ] **Step 3: 在场景 grid 与应力曲线之间插入 Section**

在 MultiField.tsx 中找到场景卡片 grid 关闭标签 `</div>` 之后、应力-应变图开始前，插入：

```tsx
        {/* PID 围压闭环伺服演示 — 机电耦合子展示 */}
        <div className="mt-6">
          <PIDServoPanel target={50} />
        </div>
```

> 由于 MultiField.tsx 较长且布局可能有 motion / AnimatePresence 包装，**实际插入点需在 Step 1 grep 后人工确认**：在场景预设 section 关闭 + 应力曲线 section 开启之间。target=50 MPa 固定，不耦合 scenario.stress。

---

### Task D.8: 浏览器验证 PID 挂载

- [ ] **Step 1: 启动 dev server**

Run:
```bash
npm run dev
```

- [ ] **Step 2: 访问 `/multifield`**

预期看到：
- 场景预设卡片（深部矿井 / 航空航天 / …）依然在
- 之后有"PID 闭环伺服调控"卡片（粉色 #F472B6 边框）
- 三档预设按钮：保守 / 经典 ZN / 激进
- Kp / Ki / Kd 三个滑块
- 响应曲线对比图（设定值 / PID 闭环 / 开环 三条线）
- 4 项指标对比卡（超调 / 稳态误差 / 调节时间 / RMSE）
- 控制律公式行

- [ ] **Step 3: 交互测试**

- 点 "保守" 预设：曲线无超调，稳态时间长。
- 点 "激进" 预设：曲线有明显超调和震荡。
- 关闭扰动 checkbox：t=3.5s 时不再有 -10 MPa 下凹。
- 拖 Kp 到 0：闭环响应应大幅退化。

- [ ] **Step 4: 停止 dev server**

---

### Task D.9: 写 docs/physics-engine/confining-pressure-pid.md

**Files:**
- Create: `docs/physics-engine/confining-pressure-pid.md`

- [ ] **Step 1: 写入文档**

Create `docs/physics-engine/confining-pressure-pid.md` with:

````markdown
# 围压 PID 闭环伺服

> 实现位置：`src/services/pidController.ts` + `src/services/confiningPressureSimulator.ts`
> UI 入口：MultiField 页面 → "PID 闭环伺服调控"卡片

## 被控对象模型

将"伺服阀 + 液压缸 + 试样腔"建模为带时滞的二阶系统：

```
τ² p̈ + 2ζτ ṗ + p = K · u(t - Td) + d(t)
```

| 符号 | 含义 | 默认值 |
|---|---|---|
| p | 实际围压 (MPa) | — |
| u | 控制器输出（归一化 0-1，伺服阀开度） | — |
| K | 静态增益（伺服阀全开稳态压力） | 200 MPa |
| τ | 时间常数 | 0.35 s |
| ζ | 阻尼比（< 1 欠阻尼，便于演示超调） | 0.45 |
| Td | 纯时滞（伺服阀响应延迟） | 0.06 s |
| d | 扰动（试样破碎瞬间泄压等） | t=3.5s 注入 -10 MPa / 0.4s |

数值积分：显式二阶欧拉，dt = 0.01 s（100 Hz 采样）。
时滞用环形缓冲实现：`uBuffer.push(u); uDelayed = uBuffer.shift()`。

## PID 控制律

标准位置式：

```
u(t) = Kp·e(t) + Ki·∫e(τ)dτ + Kd·de(t)/dt
e(t) = setpoint(t) - measured(t)
```

输出夹紧到 `[outMin, outMax]`（软防积分饱和）。

## 三档预设

| 预设 | Kp | Ki | Kd | 特征 |
|---|---|---|---|---|
| 保守 | 0.15 | 0.18 | 0.008 | 无超调，响应慢（~2 s 才到目标） |
| 经典 ZN | 0.42 | 0.70 | 0.025 | 响应快（~0.7 s），轻微超调 |
| 激进 | 0.85 | 1.20 | 0.040 | 极快但有明显超调与震荡 |

## 性能指标

| 指标 | 定义 |
|---|---|
| 超调 % | `max(measured - target) / target × 100` |
| 稳态误差 | 最后 15% 时间内 `measured - target` 平均 |
| 2% 调节时间 | 最后一次越出 `target ± 2%` 带的时刻 |
| 跟踪 RMSE | `sqrt(mean((measured - setpoint)²))` |

## 局限（必读）

1. **纯前端仿真，无真实硬件接入**：用于教学演示，**不代表**实际液压伺服阀的响应。
2. **位置式 PID**：未实现增量式与 anti-windup，长时间持续偏差时积分可能饱和；当前仅靠输出限幅软防护。
3. **K = 200 MPa 偏高**：实际典型 SHPB 围压系统 K 在 100-150 MPa，此处选 200 是为演示需要让 50 MPa 目标处于 25% 工作点。
````

---

### Task D.10: Commit D-PID

- [ ] **Step 1: 暂存**

Run:
```bash
git add src/services/pidController.ts src/services/confiningPressureSimulator.ts src/features/confining-pressure/PIDServoPanel.tsx src/pages/MultiField.tsx docs/physics-engine/confining-pressure-pid.md
```

- [ ] **Step 2: 提交**

Run:
```bash
git commit -m "feat(multi-field): PID 围压闭环伺服调控面板

实现：
- pidController.ts：位置式 PID，含输出夹紧
- confiningPressureSimulator.ts：二阶被控对象 +
  时滞环形缓冲 + 噪声 + 性能指标计算
- PIDServoPanel.tsx：三档预设 + Kp/Ki/Kd 滑块 +
  开/闭环对比曲线 + 4 项指标卡 + 控制律公式
- MultiField.tsx：在场景预设之后挂载 PIDServoPanel，target=50 MPa

文档：
- docs/physics-engine/confining-pressure-pid.md：被控对象模型 +
  PID 控制律 + 三档预设 + 性能指标定义 + 局限声明"
```

---

### Task D.11: 全量 build 校验

- [ ] **Step 1: 清理并重新构建**

Run:
```bash
npm run build
```

Expected: 退出码 0，输出含 `✓ built in ...ms`，无 TS / 模块解析错误。

- [ ] **Step 2: 检查 dist 体积**

Run:
```bash
du -sh dist/
```

Expected: dist 总大小 < 50 MB（不含 OSS 视频）。

- [ ] **Step 3: 跑 lint**

Run:
```bash
npm run lint
```

Expected: 退出码 0 或仅 warning，无 error。

---

## Phase E: Agent 审计 + 汇总

### Task E.1: 并行派 3 个 audit agents

> 此 Task 在主对话内由 Claude 用 Agent 工具一次性派出 3 个 general-purpose agents。每个 prompt 自包含，互不依赖。

- [ ] **Step 1: 在单条消息内并行调用 Agent × 3**

3 个 Agent prompt 模板：

**Agent-BP（subagent_type: general-purpose）**：

```
任务：审计 Hopkinson-Web 项目中 BP 神经网络本构预测模块的物理与算法正确性。

背景：单隐层 MLP（tansig 激活 + purelin 输出），用 J-C 参数合成训练样本，
预测应变率/温度下的 SHPB 应力-应变响应。论文来源：龙旭等 2021。

输入文件（绝对路径）：
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\services\bpNetwork.ts
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\services\bpShpbPredictor.ts
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\features\material-analysis\BPPredictionPanel.tsx

审计维度：
1. Xavier 初始化方差是否正确（sqrt(2/(in+out))）
2. tansig 导数是否使用 y 而非 x（y = tanh(x), dy/dx = 1 - y²）
3. SGD 更新方向（应 -= lr/n * grad）
4. 归一化 max==min 时是否会除零
5. 合成样本是否本质就是 J-C 复刻；R² 计算是否对多输出取平均
6. 浏览器主线程训练是否会冻结 UI

输出要求（中文）：
- P0 = 必修（科学性错误 / 数据丢失 / 崩溃）
- P1 = 应修（论文偏离 / 易误导用户的标签）
- P2 = 建议（性能 / 可读性 / 文档）
每个问题 ≤ 2 行：「文件:行号 — 一句描述 — 一句建议」
总长 ≤ 250 字。
```

**Agent-PID（subagent_type: general-purpose）**：

```
任务：审计 Hopkinson-Web 项目中 PID 围压闭环伺服模块的控制理论正确性。

背景：标准位置式 PID 控制带时滞的二阶系统（K=200, τ=0.35, ζ=0.45, Td=0.06s），
显式二阶欧拉积分，三档预设。

输入文件（绝对路径）：
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\services\pidController.ts
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\services\confiningPressureSimulator.ts
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\features\confining-pressure\PIDServoPanel.tsx

审计维度：
1. PID 离散化是否正确（积分用累加梯形或矩形）
2. 积分饱和（anti-windup）是否处理
3. 二阶系统欧拉积分稳定性（dt=0.01s 是否够细）
4. 时滞缓冲实现细节（环形缓冲 vs 简单 shift）
5. Z-N 预设值是否合理用于 K=200/τ=0.35 系统
6. 2% 调节时间定义；evalResponse 在压力始终在带内时返回值

输出要求：同 BP 审计模板（P0/P1/P2 + 文件:行号 + ≤250 字）。
```

**Agent-CT（subagent_type: general-purpose）**：

```
任务：审计 Hopkinson-Web 项目中 CT 裂隙提取模块的图像处理算法正确性。

背景：Otsu 阈值 + 形态学闭/开运算 + DFS 连通域 + 评价指标（PA/MPA/MIoU/F1）。
论文原方法为 MCSN（U-Net+VGG16），本实现为传统替代版本。

输入文件（绝对路径）：
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\services\imageProcessing\ctFissureExtractor.ts
- C:\Users\Lenovo\Desktop\work\Hopkinson-Web\src\features\ct-analysis\CTFissureExtractorPanel.tsx

审计维度：
1. Otsu 阈值实现（类间方差最大化的正确公式）
2. 形态学卷积是否处理边界（zero-padding vs reflect）
3. 连通域算法时间复杂度 + 栈溢出风险（DFS 递归 vs 迭代）
4. MIoU / MPA 是否对两个类（背景 + 裂隙）都计算后取平均
5. 上传真实 CT 时评价指标卡是否正确隐藏
6. 文档/UI 是否明确声明非论文原 MCSN

输出要求：同 BP 审计模板（P0/P1/P2 + 文件:行号 + ≤250 字）。
```

- [ ] **Step 2: 等待 3 个 agent 返回**

每个 agent 返回 ≤ 250 字报告。汇总到下一 Task。

---

### Task E.2: 汇总 audit 到一份 md

**Files:**
- Create: `docs/superpowers/audits/2026-05-12-bp-pid-ct-audit.md`

- [ ] **Step 1: 创建 audits 目录**

Run:
```bash
mkdir -p docs/superpowers/audits
```

- [ ] **Step 2: 写入汇总文档**

模板：

````markdown
# v1.1 BP / PID / CT 三模块 Agent 审计报告（2026-05-12）

> 派 3 个 general-purpose agents 并行审计，本文为汇总 + 用户决策表。

## Agent-BP 报告

> 输入：bpNetwork.ts + bpShpbPredictor.ts + BPPredictionPanel.tsx

<agent 原始回复 250 字粘贴在此>

## Agent-PID 报告

> 输入：pidController.ts + confiningPressureSimulator.ts + PIDServoPanel.tsx

<agent 原始回复 250 字粘贴在此>

## Agent-CT 报告

> 输入：ctFissureExtractor.ts + CTFissureExtractorPanel.tsx

<agent 原始回复 250 字粘贴在此>

---

## 用户决策表

| 编号 | 模块 | 严重度 | 位置 | 问题 | 建议 | 决策 |
|---|---|---|---|---|---|---|
| BP-1 | BP | P0/P1/P2 | file:line | … | … | ☐ FIX  ☐ SKIP  ☐ DEFER |
| BP-2 | BP | … | … | … | … | … |
| PID-1 | PID | … | … | … | … | … |
| CT-1 | CT | … | … | … | … | … |

（上表由 Claude 在审计后填充，用户逐行勾选 FIX / SKIP / DEFER）

---

## 修复轮次

用户勾选 FIX 的项进入下一轮，每项一个 commit：

```
fix(<scope>): <audit-id> - <一句话>
```
````

具体填充由 audit agent 输出后由 Claude 在主对话中完成。

---

### Task E.3: Commit audit 报告

- [ ] **Step 1: 暂存**

Run:
```bash
git add docs/superpowers/audits/2026-05-12-bp-pid-ct-audit.md
```

- [ ] **Step 2: 提交**

Run:
```bash
git commit -m "docs(audit): v1.1 BP/PID/CT 三模块 Agent 审计报告

- 并行派 3 个 general-purpose agents 审计：
  - BP-ANN: Xavier 初始化 / SGD / 归一化 / 浏览器线程 / R²
  - PID: 离散化 / anti-windup / 欧拉稳定性 / 时滞 / Z-N 预设
  - CT: Otsu / 形态学 / 连通域 / MIoU / 论文差异声明
- 用户决策表待逐条勾选 FIX / SKIP / DEFER"
```

---

### Task E.4: 用户决策 + 修复轮次

> 此 Task 由用户与 Claude 在主对话中协作完成。

- [ ] **Step 1: 用户审阅 audit md，逐项勾选 FIX / SKIP / DEFER**
- [ ] **Step 2: Claude 按用户勾选的 FIX 项依次实现修复**
- [ ] **Step 3: 每个修复一个 commit：`fix(<scope>): <audit-id> - <一句话>`**
- [ ] **Step 4: 全部修复后再跑 npm run build 验证**

---

## 收尾验收

### Task F.1: 端到端验收

- [ ] **Step 1: 干净 build**

Run:
```bash
npm run build
```

Expected: 退出码 0。

- [ ] **Step 2: 启动 preview 服务器**

Run:
```bash
npm run preview
```

后台运行；终端显示 `Local: http://localhost:4173/`。

- [ ] **Step 3: 跑端到端 checklist（参照 spec 全局验收）**

打开 http://localhost:4173/，依次：

- [ ] 直达 Home 不跳 /login（A 项）
- [ ] Home 见 researchHighlights 三卡（Phase 0.5）
- [ ] 顶部 5 大版块导航（首页/系统安全/材料数据/虚拟仿真/AI 中枢，Phase 0.3）
- [ ] 无用户菜单（A 项）
- [ ] /analysis → 参数拟合 tab → 模型切到 BP-ANN → 训练正常（D-BP）
- [ ] /analysis → CT 裂隙提取 tab → 提取演示 CT 正常（D-CT）
- [ ] /analysis → 参考文献 tab（岩石/混凝土材料）→ 见王登科 2024（D-CT）
- [ ] /multifield → 见 PID 伺服 Section → 三档预设切换有响应（D-PID）
- [ ] /teaching?node=any 深链跳转正常（Phase 0.5）
- [ ] 任意手机扫 public/qr-vercel.png 跳 https://hopkinson-bar.vercel.app

- [ ] **Step 4: 停止 preview**

- [ ] **Step 5: 推送到远程触发 Vercel 部署**

Run:
```bash
git push origin master
```

等待 Vercel Dashboard 显示新 deployment Ready（约 2-5 分钟）。

- [ ] **Step 6: 线上验证**

打开 `https://hopkinson-bar.vercel.app/`，再跑一遍 Step 3 的 checklist（在线版）。

---

## 任务依赖图

```
Phase 0 (Triage) ─────┐
                       │
              ┌────────┴─── A (Auth) ────┐
              │                            │
              ├──── B (QR/OSS) ────────────┤
              │                            │
              ├──── C (Cleanup) ───────────┤
              │                            │
              └──── D-BP ─── D-CT ── D-PID ┘
                                            │
                                            ▼
                                       E (Audit)
                                            │
                                            ▼
                                       F (验收 + push)
```

- Phase 0 必须最先做（隔离已有改动）。
- A / B / C / D 之间无文件冲突，可在 Phase 0 完成后并行（但顺序执行更稳）。
- E 必须在 D 全部 commit 后才能跑（agents 读已 committed 代码）。
- F 验收 + push 在 E 修复轮次后做。
